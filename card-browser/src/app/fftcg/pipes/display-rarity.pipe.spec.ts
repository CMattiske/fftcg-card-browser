import { DisplayRarityPipe } from './display-rarity.pipe';

describe('DisplayRarityPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayRarityPipe();
    expect(pipe).toBeTruthy();
  });
});
